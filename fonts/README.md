# Fonts Folder

Place project-approved font files here when caption rendering starts using a configured font folder.

Recommended file types:

- `.ttf`
- `.otf`

Recommended usage:

- add Thai-capable fonts such as `Noto Sans Thai`
- keep the final approved font set consistent across operator machines when possible
- use clear file names so later font-resolution settings are easy to trace
